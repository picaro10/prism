{
  "suppressions": [
    {
      "rule": "AGT-001",
      "file": "benchmarks/cases.ts",
      "reason": "Benchmark corpus: the vulnerable patterns exist as string content, materialized to temp projects at run time"
    },
    {
      "rule": "AGT-002",
      "file": "benchmarks/cases.ts",
      "reason": "Benchmark corpus: planted secret-in-prompt sample lives inside a string literal"
    },
    {
      "rule": "AGT-004",
      "file": "benchmarks/cases.ts",
      "reason": "Benchmark corpus: planted prompt-injection sample lives inside a string literal"
    },
    {
      "rule": "AGT-006",
      "file": "benchmarks/cases.ts",
      "reason": "Benchmark corpus: planted fail-open-gate sample lives inside a string literal"
    },
    {
      "rule": "AGT-001",
      "file": "benchmarks/ai/cases.ts",
      "reason": "AI-triage benchmark corpus: the sample lives inside a string literal, materialized to a temp project at run time (same as benchmarks/cases.ts)"
    },
    {
      "rule": "AGT-003",
      "file": "benchmarks/ai/cases.ts",
      "reason": "AI-triage benchmark corpus: the sample lives inside a string literal, materialized to a temp project at run time (same as benchmarks/cases.ts)"
    },
    {
      "rule": "AGT-004",
      "file": "benchmarks/ai/cases.ts",
      "reason": "AI-triage benchmark corpus: the sample lives inside a string literal, materialized to a temp project at run time (same as benchmarks/cases.ts)"
    },
    {
      "rule": "AGT-006",
      "file": "benchmarks/ai/cases.ts",
      "reason": "AI-triage benchmark corpus: the sample lives inside a string literal, materialized to a temp project at run time (same as benchmarks/cases.ts)"
    },
    {
      "rule": "STR-011",
      "file": "benchmarks/cases.ts",
      "reason": "Benchmark corpus: one flat file keeps every planted case and trap greppable and diffable side by side; it is data that grows by design, not logic to split"
    }
  ]
}
