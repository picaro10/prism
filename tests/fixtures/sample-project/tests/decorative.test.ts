import { describe, it } from 'vitest';

describe('decorative tests', () => {
  it('should do something', () => {
    const x = 1 + 1;
    console.log(x);
  });

  it('renders without crashing', () => {
    const component = { name: 'test' };
  });
});
